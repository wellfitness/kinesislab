# Secrets and agent configs
.claude/
.agent/
.env*
deploy.sh

# Node / test tooling
node_modules/
coverage/
test-results/
playwright-report/
/playwright/.cache/

# System files
.DS_Store
Thumbs.db

# Android build artifacts and secrets
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/gradle.properties
*.keystore
*.jks
# android/app/build.gradle SI se versiona: no contiene secretos.
# Las credenciales del keystore viven en android/gradle.properties (ignorado).

# Script auxiliar de build local y su log
COMPILAR-AAB.bat
build-log.txt
*.aab
